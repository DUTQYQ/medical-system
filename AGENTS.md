# AGENTS.md — 康养系统实训项目

大工软院 2026 秋季实习实训（9 周，2026-09-04 ~ 10-30）。**实际投入 3 人**（企划书原按 5 人规划，2026-09-08 按实际重排，见《设计决策记录 v1.0》D-06）。本项目是"基于 AI 智能体的康养系统"——面向老年人的 AI 主动式健康管理平台，**不是聊天框**。

## 一句话定位

闭环：数据采集 → AI 分析 → 健康咨询 → 异常检测 → 预警 → 家属/护工处理 → 记录。

**答辩最有说服力的场景**：老人录入异常数据后，无须主动提问，家属端便出现预警，处理后能够查到记录。

## 技术栈（已确认）

- 前端：Vue3 + Element Plus + ECharts（趋势图）；语音用浏览器 Web Speech API（【选配】，零成本）
- 后端：**Python 3.11 + FastAPI**（2026-09-08 已确认，不再考虑 Java SSM）；ORM = SQLAlchemy 2.0；校验 = Pydantic v2
- 数据库：MySQL 8
- AI：LangChain/LangGraph 编排 + 国内 LLM（DeepSeek / 通义千问 / GLM-4，**三选一仍未定**）；RAG = Embedding + Chroma/FAISS
- 换模型只改 `.env` 里的 `LLM_PROVIDER`，不改代码

## 团队与分工（3 人，按实际投入）

| 成员 | 分支 | 职责 | 备注 |
|---|---|---|---|
| 丘宇乾（组长） | `dev-qiuyu` | 后端全部 + 数据库 + AI Agent + 接口层 + 认证 + 预警中心 | **当前进度瓶颈**，一人承担 4 类工作 |
| 梁霁鸣 | `dev-liang` | 老人端 + 家属端前端 | 公共/认证页、package.json 归他 |
| 周彦龙 | `dev-zhou` | 护工端 + 管理员端前端 + 测试 | 05 集成测试目录归他 |

详细文件所有权见 `04.编码/OWNERS.md`——**分支只推迟冲突，文件所有权才避免冲突**。

## 目录结构（交付物规范，勿乱放）

```
项目根/
├── 01.需求定义/              项目企划书 v2.0
├── 02.需求分析/              需求分析说明书 v1.0（125 条 FR：核心 101 + 选配 24）
│   ├── 界面清单_康养系统_v1.0.md      38 个界面（核心 29 + 选配 9）
│   ├── 原型设计说明书_康养系统_v1.0.md  5 张效果图 + 设计说明
│   └── 界面原型/                      prototype.css + P1~P5.html + _shot.js（可重跑出图）
├── 03.概要设计/
│   ├── 设计决策记录_康养系统_v1.0.md    D-01~D-06 裁决、E-01~E-05 勘误、AI 责任边界
│   └── API接口契约_康养系统_v0.2.md    45 个接口，前后端对齐基准
├── 04.编码/前端|后端|数据库/   OWNERS.md + 各模块 README + .env.example
├── 05.集成测试/
├── 06.技术调查/
├── _归档_参考资料/            无关的遗留 Excel（勿删，保留历史）
└── 最终答辩成果物/
```

## 硬性节点

9/8 需求评审（已完成）→ 9/11 原型评审 → 9/24 概要设计+DB 评审 → 10/16 代码终版 → 10/19~23 测试 → 10/26~30 答辩（PPT+演示录屏）

## 铁律（违反会被打回）

1. **API Key 绝不进前端代码**——后端统一封装，用 .env 存，.env 在 .gitignore
2. 健康数据阈值**配置化入表（sys_config），不写死**
3. 页面必须带免责声明："健康建议不构成医疗诊断"
4. 功能分层：【核心】必做 / 【选配】增强，交付文档一律用**文字标注**、不用 emoji，选配别当必做
5. 文档一律 md 源，交付时 `pandoc x.md -o x.docx` 双份（md 进 git，docx 给老师）
6. 方案改动先列 2~3 档对比让用户拍板，别直接动手大改
7. **不得改 .gitignore**（.env.example 需纳入时用 `git add -f`）
8. AI 不得生成并执行任意 SQL、不得判断权限、不得直接写库；预警主流程不得同步等待模型（见《设计决策记录 v1.0》R-04、D-04）

## 当前进度（2026-09-08）

已完成：

- 企划书 v2.0、需求分析说明书 v1.0（125 条 FR，已正式编号）、RTM 分工
- 界面清单 v1.0（38 个界面）、5 张代表界面效果图 + 原型设计说明书
- 设计决策记录 v1.0：裁决 6 个设计缺口（注册权限、绑定授权、通知范围、AI 降级、状态分离、人员安排）
- API 接口契约 v0.2（45 个接口）
- 04.编码 目录骨架 + OWNERS.md 文件所有权 + 各模块 README + .env.example
- 3 人分支协作规范（README）

**代码仍未开始**（04.编码 下只有 README 与骨架目录），9/28 起进入编码。

## 下一步优先顺序（评审建议，已确认）

1. 9/11 前：补齐登录、绑定、指标录入、预警处理的完整交互，包括**空数据、失败、无权限**等状态
2. 9/24 前：冻结核心接口与数据关系，完成 E-R 图与建表设计
3. 9/28~10/7：**打通最小闭环**（1 老人 + 1 家属 + 1 类指标：录入 → 预警 → 通知 → 处理 → 记录）
4. 再接入 AI 与 RAG，验证回答使用真实档案，且模型不可用时业务仍能运行
5. 最后扩展【选配】：语音、导出、大屏等按剩余工期安排

## 环境速查（本机）

- Windows + PowerShell；python = C:\Users\qiuyu\QYQAnaconda\python.exe（装包走清华镜像，先清 7897 代理）
- DeepSeek/千问直连可用（NO_PROXY 已豁免），OpenAI 才需要 Clash 7897
- git 仓库 remote 在 GitHub（DUTQYQ/medical-system），**push 前先关 Clash**
- 出图：NODE_PATH 指向 workbuddy node workspace，playwright-core 用系统 Chrome 免下载浏览器
